# ClipFlow 实施进度

## 当前总览

截至 `2026-07-26`，项目已完成基础壳子搭建、阶段 3 的剪贴板历史 MVP、阶段 4 的文本转换结果复制链路，以及 macOS 开发环境可运行性验证。当前可以在 macOS 上安装依赖、构建、启动，并使用文本剪贴板历史与 `Transform` 复制能力。

## 阶段进度

### 阶段 1：项目初始化

状态：已完成

- 已初始化 `Electron + Vue3 + TypeScript + Vite` 工程
- 已建立 `main / preload / renderer / shared` 目录结构
- 已补基础类型与常量
- 已跑通 `npm run dev`

### 阶段 2：基础壳子

状态：已完成

- 已完成单窗口主界面
- 已完成 `Clipboard / Transform / Snippets` 三模块切换
- 已完成基础页面骨架和 mock 展示

### 阶段 3：剪贴板历史

状态：已完成 MVP，macOS 已手动验证

已完成：

- 在 `main` 层接入文本剪贴板轮询
- 轮询间隔固定为 `1000ms`
- 本地持久化到 `app.getPath('userData')/clipboard-history.json`
- 完成历史记录入库与应用重启恢复
- 完成重复文本去重，重复时仅刷新原记录时间
- 完成普通历史上限 `50`
- 完成置顶 / 取消置顶
- 完成复制回填系统剪贴板
- 完成删除单条记录
- 完成清空普通记录
- 完成 `preload` 受控 API 与渲染层真实数据联动
- 完成 macOS 文本剪贴板核心能力手动验证

当前固定规则：

- 仅支持文本剪贴板
- 空字符串和纯空白文本不入库
- 置顶记录不受普通记录上限裁剪影响
- 清空操作默认只清空未置顶记录
- 应用主动复制回填时会忽略本次写入，避免立即重复入库

### 阶段 4：文本转换

状态：已完成当前 MVP，继续观察交互是否需要补强

已完成：

- 输入区、动作区、输出区已完成
- 首版基础转换动作已接入纯前端逻辑
- JSON 异常提示已接入
- 转换结果复制链路已接入 `preload / Electron`
- 已补轻量行内状态反馈，区分复制成功 / 失败
- macOS 已手动验证 `Transform` 页面可用

待完成：

- 视实际使用反馈决定是否继续抽离模块状态或补充更强反馈

### 阶段 5：常用片段

状态：部分完成

功能定位：

- 面向个人单机使用的本地文本片段库
- 与 `Clipboard` 的“刚复制过什么”不同，这里管理“之后还会反复使用什么固定文本”
- 与 `Transform` 的“当前文本怎么处理”不同，这里管理可长期复用的模板和话术

已确认的离线 V1 方案：

- 本轮只实现离线 `Snippets`，不接入 Java 后端、登录或同步
- 数据由 Electron 主进程持久化到 `app.getPath('userData')/snippets.json`，并通过 `preload` 暴露受控 API
- 完成列表与关键词搜索、新增、编辑、删除、复制到系统剪贴板，以及应用重启后的数据恢复
- 标题和内容去除首尾空白后均不能为空；允许重复标题和内容
- 列表按 `updatedAt` 倒序排列；删除需二次确认，暂不提供撤销或回收站
- 沿用 `id`、`title`、`content`、`createdAt`、`updatedAt` 字段；不引入标签、置顶、分类、变量或快捷键
- 本地数据仅用于当前离线版本验证，未来接入后端时可以直接废弃，不做迁移或同步

已完成：

- 列表和搜索壳子已搭好
- mock 数据已接入

待完成：

- 新增、编辑、删除、复制
- 本地持久化
- 重启后数据恢复
- 主进程 `Snippets` 服务的加载、创建、编辑、删除、非法输入和持久化测试

明确后置：

- Java/Spring Boot、PostgreSQL 与后端 API 接入
- 登录、游客与账户数据切换、离线同步、迁移与冲突处理

### 阶段 6：工程化收尾

状态：部分完成

已完成：

- 最小 `README`
- 开发态调试能力
- macOS 依赖安装、构建、开发态启动和文本剪贴板能力验证
- macOS Electron 二进制下载恢复方式
- macOS 后续分发改造清单

待完成：

- 基础 lint
- 应用图标
- 可分发产物
- 后续 `CI/CD` 接入点

## macOS 验证情况

已完成：

- `npm run setup:electron` 可恢复 Electron 二进制安装
- `npm run build` 已在 macOS 上通过
- `npm run dev` 已在 macOS 上手动验证可打开页面
- `Command+W` 关闭窗口后，点击 Dock 栏可重新激活窗口并展示新复制内容
- `Clipboard` 和 `Transform` 在 macOS 上可使用
- 当前未发现需要在本 spec 内改造的 macOS 文本剪贴板差异

当前限制：

- macOS 公开分发尚未实施
- `.app` / `.dmg` 打包、签名、公证、自动更新、开机启动、托盘或菜单栏常驻均未实施
- 图片、文件、HTML、RTF 等非文本剪贴板历史不在当前 MVP 范围
- `Snippets` 仍是壳子和 mock 阶段，暂不支持新增、编辑、删除、复制和持久化

## 下一步计划

### 1. 常用片段离线 V1

- 实现已确认的离线 CRUD、搜索、复制与 JSON 持久化闭环
- 补齐主进程服务测试与关键界面交互验证

### 2. 文本转换交互观察

- 继续观察当前轻量状态反馈是否已足够支撑日常使用
- 视复杂度决定是否抽离模块状态

### 3. 工程化收尾

- 补基础 lint
- 配置应用图标
- 评估打包分发方式
- 预留 `CI/CD` 接入点

### 4. 后续 spec 候选

- macOS 公开分发：`.app` / `.dmg`、签名、公证、自动更新
- 深度剪贴板能力：图片、文件、HTML、RTF 等非文本剪贴板历史
- 全局快捷键、托盘或菜单栏常驻、开机启动

## 推进原则

- 优先闭环，不扩功能
- 优先最小可运行实现，不提前做复杂抽象
- 以后续实际使用反馈决定是否继续加搜索、标签、快捷键等能力
