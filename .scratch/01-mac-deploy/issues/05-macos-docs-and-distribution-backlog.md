# 05 — 补充 macOS 开发运行文档与后续分发清单

**构建内容：** 让项目文档明确说明 macOS 当前支持能力、启动步骤、常见问题、已验证命令，以及后续面向普通用户分发时需要补齐的工作。

**阻塞关系：** 02 — 验证 macOS 构建链路；03 — 验证 macOS 开发态启动；04 — 验证 macOS 文本剪贴板核心能力。

**状态：** done

- [x] 文档说明 macOS 当前支持的文本剪贴板能力。
- [x] 文档说明 macOS 本地开发启动步骤。
- [x] 文档记录 Electron 二进制下载失败、依赖安装失败、可执行权限异常等常见问题的恢复方式。
- [x] 文档明确本次不包含公开分发能力。
- [x] 文档列出后续 macOS 分发改造清单，包括打包、签名、公证、自动更新、开机启动、托盘或菜单栏行为。
- [x] 文档说明哪些深度剪贴板能力应拆到后续 spec。

## 验证记录

- `README` 已补充 macOS 当前支持能力与分发范围说明。
- 项目使用文档已补充 macOS 启动、构建、剪贴板能力、常见问题和后续分发清单。
- 本次明确不包含 macOS 公开分发能力；`.app` / `.dmg` 打包、签名、公证、自动更新、开机启动、托盘或菜单栏行为均进入后续改造清单。
- 本次明确不包含图片、文件、HTML、RTF 等非文本剪贴板历史；这些能力应拆到后续 spec。
